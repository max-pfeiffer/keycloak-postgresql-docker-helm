apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "keycloak-postgresql.fullname" . }}-test-connection"
  labels:
    {{- include "keycloak-postgresql.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "keycloak-postgresql.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
